namespace CanonFlow.Evaluator

module Policy =
    // Placeholder for parsing and validating protected policies and ONDC profiles
    let validateProfileDigest (expected: string) (actual: string) =
        expected = actual

